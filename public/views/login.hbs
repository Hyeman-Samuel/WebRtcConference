<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log In Page</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
    <link rel="stylesheet" type="text/css" href="/public/css/auth_style.css"/>
</head>
<body>
    <div class="container">
        <div class="row signUp">
            <div class="col-md-4 offset-md-2">
                <h1>Log In</h1>
            </div>
            <div class="register col-md-5">
            {{#if errors }}
                {{#each errors }}
                    <div class="alert alert-secondary" role="alert">{{ this.msg }}</div>
               {{/each}}
            {{/if}}
                
                <form action="/user/login"  class="form-group inputField" method="post">
                    <input class="form-control" name="Username" placeholder="Username">
                    <input class="form-control" name="Password" placeholder="Password ">
                    <button class="btn-success" >Submit</button>
                </form>
               

            </div>
        </div>
    </div>
</body>
</html>